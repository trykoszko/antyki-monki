{% if headerNav %}
    <ul class="c-footer__nav">
        {% for item in headerNav.items %}
            <li class='c-footer__nav-item{% if item.current %} is-active{% endif %}{{ item.classes|join(' ') }}'>
                <a href="{{ item.link }}" class="c-link">
                    <span class="c-label">
                        {{ item.title }}
                    </span>
                </a>
            </li>
        {% endfor %}
    </ul>
{% endif %}
